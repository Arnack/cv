import React from "react";
import styles from './nav.module.scss';

export const Nav = () => {
    return (
        <nav className={styles.downloadCV}>
            <span>Download CV</span>
        </nav>
    );
}